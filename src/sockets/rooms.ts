export const userRoom = (userId: number) => `user:${userId}`;
export const companionRoom = (companionProfileId: number) => `companion:${companionProfileId}`;
export const bookingRoom = (bookingId: number) => `booking:${bookingId}`;
